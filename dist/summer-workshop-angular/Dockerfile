# Utilisez une image Nginx comme image de base
FROM nginx:latest

# Copiez les fichiers de build Angular depuis le répertoire de build vers le répertoire de travail de Nginx
COPY /dist/summer-workshop-angular /usr/share/nginx/html

# Nginx est configuré pour écouter le port 80 par défaut
EXPOSE 80 
#tt
